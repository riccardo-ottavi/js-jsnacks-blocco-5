const names = ['Edoardo', 'Simone', 'Francesco'];

// Stampa in console tutti i nomi
// Risultato: 'Edoardo', 'Simone', 'Francesco'

//ciclo l'array e via via mando in console la proprty richiesta
for(let i = 0; i < names.length; i++){
    console.log(names[i]);
}

